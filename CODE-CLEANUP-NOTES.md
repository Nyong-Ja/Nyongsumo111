# NyongJa Phasmophobia — 코드/디자인 통합 정리본

## 이번 버전
- 기존 사이트의 딥 네이비/블랙 + 퍼플 아이덴티티 유지
- Premium UI 스타일 적용본을 기준으로 코드 정리
- 기존 HTML/JS 데이터 구조와 기능을 최대한 보존
- CSS/JS/HTML의 줄 끝 공백 및 줄바꿈 통일
- CSS에서 반복되는 일부 연속 중복 선언 정리
- `!important`는 기능 영향 가능성이 있어 일괄 삭제하지 않음

## 중요한 원칙
`!important`는 숫자를 줄이는 것 자체가 목적이 아니라,
각 규칙의 우선순위를 정상적인 선택자 구조로 바꿀 수 있을 때 단계적으로 제거해야 합니다.

다음 리팩터링 단계에서는:
1. CSS 중복 선택자 통합
2. 실제 충돌 규칙 분석
3. 불필요한 `!important` 제거
4. JS 중복 함수/죽은 코드 점검
5. 파일별 역할과 섹션 정리
순으로 진행하는 것이 안전합니다.

